package model.objects.ghosts;

import model.objects.Field;
import util.Pair;

public class Blinky extends Ghost implements GhostProtocol {
    public Blinky(Field field) {
        super(field);
    }

}
