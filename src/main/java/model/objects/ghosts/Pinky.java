package model.objects.ghosts;

import model.objects.Field;
import util.Pair;

public class Pinky extends Ghost implements GhostProtocol{
    public Pinky(Field field) {
        super(field);
    }

}
