package model.objects.ghosts;

import model.objects.Field;
import util.Pair;

public class Clyde extends Ghost implements GhostProtocol{
    public Clyde(Field field) {
        super(field);
    }

}
