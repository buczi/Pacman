package body;

import model.Map;
import model.objects.*;
import model.objects.ghosts.*;
import util.Direction;

public class Application {
    public static void main(String[] args){
    /*
            Map.moveGhost(new Blinky(new Field(0,0, FieldType.Blinky)), Direction.Up);
        Map.moveGhost(new Clyde(new Field(0,0, FieldType.Clyde)), Direction.Up);
        Map.moveGhost(new Inky(new Field(0,0, FieldType.Inky)), Direction.Up);
     */
    }
}
